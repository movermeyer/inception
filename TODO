project structure:
  - configuration.py  <- configuration.
  - templates/        <- templates, with structure.

configuration:
  - just return configuration variables:
    - dependencies
    - questions